# Python 3.8 打包依赖清单
# TCL表格比对系统 - EXE打包专用

# === GUI框架（必须）===
PyQt5==5.15.9

# === Excel处理（必须）===
openpyxl==3.1.2

# === 网络功能（可选 - 局域网模式需要）===
flask==2.3.3
flask-cors==4.0.0
requests==2.31.0

# === 打包工具（仅打包时需要）===
pyinstaller==5.13.2

# === 可选依赖（如果代码中使用）===
# pandas==2.0.3          # 数据分析（如果使用）
# numpy==1.24.4         # 数值计算（如果使用）
